# 🚀 QUICK INSTALLATION COMMANDS

## Copy and paste these commands one by one:

### 1. Install Node.js Dependencies
```bash
cd backend
npm install
cd ..
```

### 2. Setup Python Virtual Environment
```bash
cd biometric-service
python -m venv venv
venv\Scripts\activate
```

### 3. Install Python Packages
```bash
pip install -r requirements.txt
```

**Note:** This will take 5-10 minutes. It automatically downloads face recognition models.

### 4. Start Services
```bash
cd ..
START_BIOMETRIC.bat
```

### 5. Test
- Open: http://localhost:5000
- Register → Login → Console: `enrollFace()` → Logout → Click "Face" button

---

## ✅ DONE!

No manual model downloads needed! Everything installs automatically.
